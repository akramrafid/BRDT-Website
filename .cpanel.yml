---
deployment:
  tasks:
    # This copies everything inside the frontend/public folder into your public_html folder.
    # IMPORTANT: Replace "YOUR_CPANEL_USERNAME" with your actual cPanel username!
    - export DEPLOYPATH=/home/YOUR_CPANEL_USERNAME/public_html/
    - /bin/cp -R frontend/public/* $DEPLOYPATH
